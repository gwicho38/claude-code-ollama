# litellm proxy — Anthropic Messages API frontend for Ollama
#
# Deploy on the same host as Ollama (192.168.8.239).
# Nginx on 192.168.8.1 reverse-proxies litellm.lefv.info → this service.
#
# Usage:
#   docker compose up -d
#   docker compose logs -f litellm
#   docker compose down

services:
  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    container_name: litellm-proxy
    restart: unless-stopped
    ports:
      - "4000:4000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    command:
      - "--config"
      - "/app/config.yaml"
      - "--port"
      - "4000"
      - "--num_workers"
      - "2"
    # Ollama runs on the host — litellm needs host network access
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - OLLAMA_API_BASE=http://host.docker.internal:11434
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:4000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
